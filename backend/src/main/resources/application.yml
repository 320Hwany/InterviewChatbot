spring:
  h2:
    console:
      enabled: true

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:interviewChatbot
    username: sa
    password:

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
